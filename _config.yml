# GitHub Pages Configuration
# This file is optional but helps configure GitHub Pages behavior

# Theme (using default GitHub Pages theme, but we have custom CSS)
theme: null

# Include/exclude files
include:
  - css
  - js
  - data
  - "*.html"

# Exclude unnecessary files from the built site
exclude:
  - README.md
  - .gitignore
